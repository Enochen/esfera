using UnityEngine;
using static InputManager;

public class BoxerSkill0 : Skill
{
  public BoxerSkill0(SkillMeta meta, int cooldown = 0) : base(meta, cooldown) {
  }

  public override void Activate() {
    base.Activate();
  }
}
